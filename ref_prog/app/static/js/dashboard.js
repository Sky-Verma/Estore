    // document.querySelector('.profile_pic_p').innerHTML = `<input type="file" name="profile_pic" accept="image/*" id="id_profile_pic">`;
    // var textarea = document.querySelector('textarea');
    // textarea.rows = 2;
    // textarea.cols = 20;

    var code = document.querySelector('.code');

    code.textContent = window.location.host + '/code/' + code.textContent;
